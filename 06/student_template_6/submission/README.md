compilation may take a while, as `your_shape()` in `submission/your_shape.cpp` 
is quite complicated and big.

i generated the shape calls by first creating a black and white image. then, i 
wrote a small script that read the image, which then generates the shape calls 
from it.

when compiled and run, you should see the face of a cute fox :)