#pragma once

/**
 * @brief Runs the code of exercise00.
 *
 * Prints some pretty output to the terminal.
 */
void run_exercise00();

/**
 * @brief Adds two numbers and returns the result.
 * 
 * @param a First summand 
 * @param b Second summand
 * @return int The sum of a and b.
 */
int add(int a, int b);